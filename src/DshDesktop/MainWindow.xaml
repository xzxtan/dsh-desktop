<Window x:Class="DshDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="DeepSeek Harness"
        Icon="/assets/app.ico"
        Background="#151517"
        Width="1280" Height="800"
        WindowStyle="None"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
    <WindowChrome.WindowChrome>
        <!-- 全出血：无标题条；仅保留 6px 缩放边框与系统贴靠行为。
             CornerRadius=0：圆角由代码自管（见 ApplyCornerRounding）——
             WindowChrome 的 CornerRadius 会在拖动/缩放循环里逐帧重建窗口区域，造成掉帧。 -->
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="Transparent" />
            <!-- 页面主题令牌：label-primary / bg-layer-2 / bg-layer-1 -->
            <Setter Property="Foreground" Value="#F9FAFB" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2C2C2E" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3A3A3C" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TitleBarCloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- state-error-primary -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F25A5A" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF6B6B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- 6px 内容内边距：露出窗口背景（页面同色，视觉隐形），
             该环带由 WindowChrome 原生命中测试接管，提供边缘缩放与 Aero 贴靠。
             WebView2 子窗口盖满客户区会吞掉父窗口的 NC 命中，这是必须留边的原因。
             注意：边距必须直接挂在 WebView2 控件上——HwndHost 不吃父容器边距。 -->
        <wv2:WebView2 x:Name="Browser" Margin="6" Visibility="Collapsed" />

        <!-- 离线/启动覆盖层：左上品牌、右上窗口按钮、中央状态 -->
        <Border x:Name="Overlay" Background="#151517" Margin="6"
                MouseLeftButtonDown="Overlay_MouseLeftButtonDown">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top"
                            Margin="14,10,0,0">
                    <Image Source="/assets/tray-online.png" Width="18" Height="18"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                    <TextBlock Text="DeepSeek Harness" Foreground="#C8CDD6" FontSize="13"
                               VerticalAlignment="Center" Margin="8,0,0,0" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button Style="{StaticResource TitleBarButton}" Content="&#xE921;"
                            ToolTip="最小化" Click="Minimize_Click" />
                    <Button x:Name="MaximizeButton" Style="{StaticResource TitleBarButton}" Content="&#xE922;"
                            ToolTip="最大化 / 还原" Click="Maximize_Click" />
                    <Button Style="{StaticResource TitleBarCloseButton}" Content="&#xE8BB;"
                            ToolTip="关闭" Click="CloseWindow_Click" />
                </StackPanel>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock x:Name="OverlayTitle" Text="正在启动 DeepSeek Harness…"
                               FontSize="20" Foreground="White" HorizontalAlignment="Center" />
                    <TextBlock x:Name="OverlayDetail" Text="正在连接后端…"
                               Foreground="#B0FFFFFF" HorizontalAlignment="Center" Margin="0,8,0,0" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <Button x:Name="RetryButton" Content="重试" Width="96" Margin="0,0,8,0"
                                Visibility="Collapsed" Click="Retry_Click" />
                        <Button Content="打开设置" Width="96" Click="Settings_Click" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
